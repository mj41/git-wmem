# UC: user-sh-cmds commit-msg-prefix-file

User sets commit message for wmem operations.

1) User sets commit message for the next wmem commit:
    ```sh
    > cd ~/work/my-wmem1
    > echo "projA and projB features" > md/commit/msg-prefix
    ```
