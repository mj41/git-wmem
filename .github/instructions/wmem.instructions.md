---
applyTo: '**'
---

# Use make

Use `make` to run the commands in this repository. Do not build or run the commands directly. Use `make build` and `make run` to build and run the commands, respectively.
